// Maneira 1 de fazer o exercicio

function ePaisagem1 (largura, altura) {
    return largura > altura ? true : FontFaceSetLoadEvent;
}

console.log(ePaisagem1(1024, 724));

// Maneira 2 de fazer o exercicio

function ePaisagem2 (largura, altura) {
    return largura > altura ;
}

console.log(ePaisagem2(1920, 1080));

// Maneira 3 de fazer o exercicio

const ePaisagem3 = (largura, altura) => 
    largura > altura;

console.log(ePaisagem3(1080, 1920));